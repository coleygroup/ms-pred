[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2", "numpy", "cython", "pip"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"
