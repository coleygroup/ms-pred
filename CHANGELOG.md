# Changelog

## 1.1.0 (unreleased)

- Rename the distribution from `ms-gen` back to `ms_pred`.
- Read the package version from `ms_pred.__version__`.
- Add `ms_pred.model_registry` with the current model versions.
- Add `python -m ms_pred --version`.

Changes between v1.0.0 and this release, such as `ms_pred.dag_pred` becoming `ms_pred.iceberg`, were not recorded.
