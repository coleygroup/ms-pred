# Changelog

## Unreleased

- Rename the distribution from `ms-gen` to `ms_pred`.
- Read the package version from `ms_pred.__version__`.
- Add `ms_pred.model_registry` to record model versions separately from the package version.
